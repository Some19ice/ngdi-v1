/**
 * Configuration for dynamic routes
 * 
 * This file contains configuration for routes that need to be rendered dynamically
 * because they use server-side features like headers, cookies, or searchParams.
 */

// Force dynamic rendering for routes
export const dynamic = 'force-dynamic';
